{
  "scripts": {
    "build": "echo 'Static build complete'",
    "deploy": "wrangler pages deploy ."
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.7",
    "@supabase/supabase-js": "^2.116.0"
  }
}

